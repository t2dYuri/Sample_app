<%= link_to gravatar_for(current_user, size: 100), current_user %>
<h1 style="text-align: center">
  <%= current_user.name %>
</h1>
<span style="margin-top: 20px; text-align: center">
  <%= link_to "Просмотр профиля", current_user, class: "btn btn-default btn-xs" %>
</span>
<span style="margin-top: 22px; text-align: center">
  <strong><%= pluralize(current_user.microposts.count, "micropost") %></strong>
</span>